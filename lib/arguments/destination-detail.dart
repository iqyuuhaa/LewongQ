class DestinationDetailArguments {
  final tag;
  final uname;
  final picture;
  final name;
  final description;
  final city;
  final address;
  final maps;

  DestinationDetailArguments(this.tag, this.uname, this.description, this.name,
      this.picture, this.address, this.city, this.maps);
}
