class GalleryDetailArguments {
  final tag;
  final uname;
  final picture;
  final name;
  final description;

  GalleryDetailArguments(this.tag, this.uname, this.picture, this.name, this.description);
}